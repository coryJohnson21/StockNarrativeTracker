DATABASE_URL=postgresql+asyncpg://narrativetracker:narrativetracker@localhost:5432/narrativetracker
OPENAI_API_KEY=sk-...
CORS_ORIGINS=http://localhost:3000
ENVIRONMENT=development

# Optional: Reddit "script" app from https://www.reddit.com/prefs/apps. Enables the
# official API (posts + top comments). Without these, Reddit ingestion falls back to
# the public Atom feed (posts only, no comments).
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
